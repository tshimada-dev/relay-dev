# Provider Smoke Run Monitoring - 2026-05-13

## Scope

- Run: `run-20260512-164852`
- Task: `tasks/task.md` (`実運用前の Provider Smoke Test 導入タスク`)
- Canonical sources:
  - `runs/current-run.json`
  - `runs/run-20260512-164852/run-state.json`
  - `runs/run-20260512-164852/events.jsonl`
  - `pwsh -NoLogo -NoProfile -File .\app\cli.ps1 show`

## Occurred

| Time | Issue | Evidence | Impact |
| --- | --- | --- | --- |
| 2026-05-12 17:25 JST | Phase3 provider job timed out and did not produce `phase3_design.md` / `phase3_design.json`. | `events.jsonl` contains `run.failed` with `failure_class: timeout` after `job-20260512170531-phase3-implementer`. | Run stopped in Phase3 until recovery. |
| 2026-05-12 17:25 JST -> 2026-05-13 13:04 JST | Retried Phase3 job remained as stale `active_job_id`. | `cli.ps1 show` reported `job-20260512172539-phase3-implementer` as `stale: true` with `lease_expired,heartbeat_stale`. | Capacity was full and no new job could launch until resume recovery. |
| 2026-05-13 09:57 JST | Unexpected `fake-provider` Phase1 job events appear in the run event log. | `events.jsonl` contains `job.dispatched` / `job.started` for `job-20260513095729-phase1-implementer` with `provider: fake-provider`. | No run-state impact observed yet; event-log provenance should be watched. |
| 2026-05-13 13:19 JST | Phase3-1 reviewer returned `conditional_go` with two carry-forward requirements. | `phase3-1_verdict.json` and `events.jsonl` list must-fix items for stdin sentinel observation and a single canonical smoke evidence path. | Run returned to Phase3 with `open_requirements`; no manual blocker remains. |
| 2026-05-13 13:31 JST | Second Phase3-1 reviewer pass returned another `conditional_go` with two additional carry-forward requirements. | `phase3-1_verdict.json` and `events.jsonl` list must-fix items for fixed `logs/provider-smoke/<smoke_id>/result.json` indexing and `-Provider` / `-ProviderCommand` consistency. | Run returned to Phase3 with four total `open_requirements`; no manual blocker remains. |
| 2026-05-13 13:44 JST | Third Phase3-1 reviewer pass returned `conditional_go` with an execution-runner isolation requirement. | `phase3-1_verdict.json` and `events.jsonl` identify that `Invoke-ExecutionRunner` currently falls back to `Resolve-ActiveRunId`, which can write smoke activity into canonical run events/metadata. | Run returned to Phase3 with five total `open_requirements`; no manual blocker remains. |
| 2026-05-13 14:00 JST | Fourth Phase3-1 reviewer pass returned `conditional_go` with a provider-independent prompt success contract requirement. | `phase3-1_verdict.json` and `events.jsonl` require `ProviderSmokePromptContract` to define how `stdin_verified` is established without relying on provider prompt echo. | Run returned to Phase3 with six total `open_requirements`; no manual blocker remains. |
| 2026-05-13 14:13 JST | Fifth Phase3-1 reviewer pass returned `conditional_go` with provider-resolution and direct-smoke documentation requirements. | `phase3-1_verdict.json` and `events.jsonl` require smoke provider resolution to default to `config/settings.yaml` `cli.command` / `cli.flags`, and require docs to name `scripts/provider-smoke.ps1` as the only direct execution-primitives exception. | Run returned to Phase3 with eight total `open_requirements`; no manual blocker remains. |
| 2026-05-13 14:44 JST | Phase4-1 reviewer returned `conditional_go` with two task-contract coverage gaps. | `phase4-1_verdict.json` requires T-02 to fix the `-OutputRoot`/`result.json` acceptance contract and T-01 or T-02 to own raw-log redaction verification. | Run returned to Phase4 with ten total `open_requirements`; no manual blocker remains. |
| 2026-05-13 14:57 JST | Second Phase4-1 reviewer pass returned `conditional_go` with live-smoke and parallel-lane ownership gaps. | `phase4-1_verdict.json` requires a task owner for one manual/opt-in live provider smoke run and an owner or explicit defer note for parallel-capable lane verification. | Run returned to Phase4 with twelve total `open_requirements`; no manual blocker remains. |
| 2026-05-13 15:11 JST | Parallel mode was requested after Phase5 T-01 had already started under the single-lane worker. | `cli.ps1 step -ConfigFile config/settings.parallel.local.yaml` was blocked by `run.lock`, and `cli.ps1 show` still reported `task_lane.mode: single`, `max_parallel_jobs: 1`. | T-01 work in progress was intentionally discarded so the run could restart from T-01 with parallel-capable config. |
| 2026-05-13 15:24 JST | Phase4 task breakdown said `T-01/T-02` were parallel candidates, but run-state still blocked `T-02` on `T-01`. | Current `phase4_tasks.json` has `T-01.dependencies: []` and `T-02.dependencies: []`, while `run-state.json` still had stale `T-02.depends_on: [T-01]`, `T-03.depends_on: [T-02]`, and `T-04.depends_on: [T-02]`. | Scheduler correctly followed stale run-state, so only T-01 launched. |
| 2026-05-13 15:33 JST | Using normal `step` after reset fell back to a single-step T-01 job. | `events.jsonl` shows `job-20260513153351-phase5-implementer` leased with `lease_owner: single-step` even though `task_lane.mode` was `parallel`. | T-01 had to be stopped again and relaunched through `group-step` directly. |
| 2026-05-13 15:40 JST | First task-group T-02 worker failed with a provider error because its working directory pointed at the copied source root instead of the copied repo root. | `task-worker-5981f911402d0843` stderr attempted to read `app/execution/execution-runner.ps1` from `runs/.../workspaces/task-worker-5981f911402d0843`, while the copied repo was under `...\task-worker-5981f911402d0843\relay-dev`; the job then ended with `failure_class: provider_error`. | The first task group was reset and discarded; T-01/T-02 needed a clean restart after fixing isolated workspace path resolution. |
| 2026-05-13 15:50 JST | A recovery launch accidentally resumed through the normal single-step path again. | `events.jsonl` shows `job-20260513155022-phase5-implementer` leased by `single-step` and then failed artifact validation because `phase5_implementation.md` / `phase5_result.json` were not produced. | The visible agent loop and stale lock were stopped/cleared; run-state was reset again before using `group-step` only. |
| 2026-05-13 16:03 JST | Cleanly restarted T-02 worker failed on provider capacity. | `task-worker-c64054929208d56e` used the corrected `...\workspaces\task-worker-c64054929208d56e\relay-dev` working directory and began the T-02 plan, then stderr ended with `ERROR: Selected model is at capacity. Please try a different model.` | This is an external provider-capacity blocker, not the earlier workspace-path bug. The active task group could not fully succeed unless T-02 was retried after capacity recovered. |
| 2026-05-13 16:08 JST | T-01 was stopped after its sibling T-02 failed, to avoid continuing an all-or-nothing group that could not merge. | `task_group.operator_reset` was appended with `reason: provider_capacity_retry_deferred`; `task-group-269f5489f7dc9f76` is marked `partial_failed`; T-01/T-02 are reset to `ready`. | The run is clean for a delayed retry from T-01/T-02 after provider capacity recovers. |
| 2026-05-13 16:28 JST | `status: running` with no `active_job_id` looked like a stalled hidden job. | `run-state.json` had `status: running`, empty `active_jobs`, no `run.lock`, no pending approval, and T-01/T-02 in the ready queue. `show` marked both ready rows as `cautious_parallel_safety`. | Canonical state was not corrupt; the run was awaiting explicit cautious parallel dispatch. UI/summary wording was too weak for operators. |
| 2026-05-13 16:43 JST | T-01/T-02 were restarted as a cautious parallel task group. | Operator launched `group-step -RunId run-20260512-164852 -ConfigFile config/settings.parallel.local.yaml -AllowCautiousParallelJob`; events show `task_group.packaged` for `task-group-68b1d021fc83947d`, then `job.started` for both workers. | Run is active again in Phase5 with two task-group workers. Continue monitoring for provider capacity or artifact failures. |
| 2026-05-13 16:54 JST | The second cautious task-group restart failed on provider capacity and left stale group state behind. | T-02 worker `task-worker-6f3027a0f92b2d30` finished with `failure_class: provider_error` and stderr `Selected model is at capacity`; T-01 worker `task-worker-73f5e2b144df7749` had a stale heartbeat and no live worker process. | Provider capacity remains an external blocker; the stale group state needed local recovery. |
| 2026-05-13 17:19 JST | The current task group was marked stale, but T-01/T-02 initially remained `in_progress`. | `Repair-StaleTaskGroupWorkerState` existed but was not wired into the `group-step` recovery path, and it did not clean task states still pointing at a stale/failed task group. | Added a code fix so stale task-group recovery can return affected tasks to the ready queue. |
| 2026-05-13 17:29 JST | Stale task-group recovery was applied without launching workers. | `group-step` was run without `-AllowCautiousParallelJob`; it repaired `task-group-68b1d021fc83947d` to `stale` and returned T-01/T-02 to `ready`, then stopped at `awaiting_cautious_parallel_opt_in`. | Run is clean for a delayed cautious retry, but should not auto-retry while provider capacity is failing. |

## Resolved

| Time | Resolution | Evidence |
| --- | --- | --- |
| 2026-05-13 before resume | Timeout retry handling was fixed and pushed to `main`. | Commit `d11f035 Fix execution runner timeout retry handling`; parent verification passed `tests/regression.ps1`, `tests/task-parallelization-headless-execution.ps1`, parse checks, `git diff --check`, and focused timeout checks. |
| 2026-05-13 13:04 JST | Current run was resumed from the previous state with `start-agents.ps1 -ResumeCurrent`. | Launcher reported `Worker launched successfully`; `events.jsonl` contains `job.recovered` for stale `job-20260512172539-phase3-implementer`. |
| 2026-05-13 13:04 JST | A fresh Phase3 job was launched. | `cli.ps1 show` reports active job `job-20260513130443-phase3-implementer`, `stale: false`, `current_phase: Phase3`, `current_role: implementer`, and no pending approval. |
| 2026-05-13 13:04 JST | Continuous monitoring was scheduled. | Thread heartbeat automation `monitor-relay-dev-run` was created with a 15-minute interval. |
| 2026-05-13 13:13 JST | Phase3 design completed after resume. | `events.jsonl` shows `job-20260513130443-phase3-implementer` finished with `result_status: succeeded`; `phase3_design.md` and `phase3_design.json` validated and committed. |
| 2026-05-13 13:19 JST | Phase3-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` with `decision: approve`, followed by transition back to Phase3 with carry-forward `open_requirements`. |
| 2026-05-13 13:26 JST | First Phase3 follow-up design revision completed. | `events.jsonl` shows transition through Phase3 to Phase3-1; reviewer accepted the sentinel/evidence-path direction but added two sharper public-contract requirements. |
| 2026-05-13 13:32 JST | Second Phase3-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` for `approval-20260513133144`, followed by transition back to Phase3 with four carry-forward `open_requirements`. |
| 2026-05-13 13:39 JST | Second Phase3 follow-up design revision completed. | `events.jsonl` shows Phase3 transition to Phase3-1; reviewer accepted the prior contract fixes but added runner isolation as a remaining design requirement. |
| 2026-05-13 13:45 JST | Third Phase3-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` for `approval-20260513134439`, followed by transition back to Phase3 with five carry-forward `open_requirements`. |
| 2026-05-13 13:52 JST | Third Phase3 follow-up design revision completed. | `events.jsonl` shows Phase3 transition to Phase3-1; reviewer accepted runner isolation direction but added a provider-independent success response contract requirement. |
| 2026-05-13 14:00 JST | Fourth Phase3-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` for `approval-20260513140002`, followed by transition back to Phase3 with six carry-forward `open_requirements`. |
| 2026-05-13 14:07 JST | Fourth Phase3 follow-up design revision completed. | `events.jsonl` shows Phase3 transition to Phase3-1; reviewer accepted the provider-independent prompt contract direction but added provider-resolution and direct-smoke docs requirements. |
| 2026-05-13 14:13 JST | Fifth Phase3-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` for `approval-20260513141340`, followed by transition back to Phase3 with eight carry-forward `open_requirements`. |
| 2026-05-13 14:22 JST | Fifth Phase3 follow-up design revision completed. | `events.jsonl` shows Phase3 transition to Phase3-1 after `job-20260513141352-phase3-implementer` succeeded. |
| 2026-05-13 14:31 JST | Phase3-1 reviewer returned `go` and Phase4 started. | `phase3-1_verdict.json` has `verdict: go` with empty `must_fix`; `events.jsonl` shows `approval.resolved` for `approval-20260513143105`, transition to Phase4, and active job `job-20260513143120-phase4-implementer`. |
| 2026-05-13 14:38 JST | Initial Phase4 task breakdown completed. | `events.jsonl` shows `job-20260513143120-phase4-implementer` succeeded and `phase4_task_breakdown.md` / `phase4_tasks.json` were validated and committed. |
| 2026-05-13 14:44 JST | Phase4-1 conditional approval was resolved. | `events.jsonl` shows `approval.resolved` for `approval-20260513144423`, followed by transition back to Phase4 with two new Phase4 carry-forward requirements. |
| 2026-05-13 14:51 JST | Phase4 task-contract revision completed. | `cli.ps1 show` reports transition from Phase4 to Phase4-1 after `job-20260513144447-phase4-implementer`; `phase4_tasks.json` now defines five tasks (`T-01` through `T-05`). |
| 2026-05-13 14:57 JST | Second Phase4-1 conditional approval was resolved. | `events.jsonl` shows transition back to Phase4 with two additional Phase4 carry-forward requirements. |
| 2026-05-13 15:04 JST | Second Phase4 task-contract revision completed. | `cli.ps1 show` reports transition from Phase4 to Phase4-1 after `job-20260513145725-phase4-implementer`; current `phase4_tasks.json` assigns manual live smoke to `T-03` and parallel-capable run documentation/verification to `T-05`. |
| 2026-05-13 15:19 JST | Run was restarted from T-01 with parallel-capable local config. | The old single-config worker/provider were stopped, stale lock was removed, and `start-agents.ps1 -ResumeCurrent -ConfigFile config/settings.parallel.local.yaml` launched a visible worker. `events.jsonl` contains `task_lane.configured` with `mode: parallel`, `max_parallel_jobs: 2`; `cli.ps1 show` reports active T-01 job `job-20260513151922-phase5-implementer`. |
| 2026-05-13 15:27 JST | Stale task dependency resync was fixed. | `app/core/workflow-engine.ps1` now refreshes existing non-completed planned task `depends_on` / `task_contract_ref` from current `phase4_tasks.json`; `tests/task-parallelization-scheduler.ps1` covers a stale dependency becoming ready after Phase4 artifact resync. Focused test passed. |
| 2026-05-13 15:37 JST | T-01 and T-02 were relaunched as a two-worker task group. | `events.jsonl` contains `task_group.packaged` for `task-group-71bacc353aeeaa17` with `worker_count: 2`; `run-state.json` shows `T-01` and `T-02` as `in_progress` with `wait_reason: task_group_running`. Worker processes are `task-worker-9228cc54158a67c0` (T-01) and `task-worker-5981f911402d0843` (T-02). |
| 2026-05-13 15:49 JST | The failed first task group was operator-reset after the isolated workspace path issue was identified. | `events.jsonl` contains `task_group.operator_reset` with `reason: isolated_workspace_path_fix`; `task-group-71bacc353aeeaa17` is now `partial_failed` and no longer active. |
| 2026-05-13 15:58 JST | Isolated workspace path resolution was fixed and covered by tests. | `app/core/parallel-workspace.ps1` now resolves `workspace_path` to the copied project subdirectory when `ProjectRoot` is inside `SourceWorkspace`; `tests/task-group-parallel-package.ps1` asserts nested source workspaces expose project files from `...\worker\relay-dev`. Passed: `tests/task-group-parallel-package.ps1`, `tests/task-parallelization-scheduler.ps1`, `tests/task-group-parallel-worker-loop.ps1`. |
| 2026-05-13 16:01 JST | T-01 and T-02 were relaunched cleanly as a new two-worker task group. | `events.jsonl` shows `task_group.packaged` for `task-group-269f5489f7dc9f76`, followed by `job.started` for `task-worker-c64054929208d56e` (T-02) and `task-worker-509c3fc7b9610faa` (T-01). Both worker `workspace_path` values end in `\relay-dev`. |
| 2026-05-13 16:33 JST | Running-but-idle operator visibility was improved. | `task-lane-summary.ps1` no longer counts `partial_failed` task groups as active and now exposes `dispatch_state`, including `awaiting_cautious_parallel_opt_in`. `run-summary-renderer.ps1` and `dashboard-renderer.ps1` render that dispatch state. Passed: `tests/task-parallelization-ready-ui.ps1`, `tests/task-group-parallel-ui.ps1`, `tests/task-parallelization-scheduler.ps1`. |
| 2026-05-13 16:44 JST | T-01/T-02 cautious parallel restart is active. | `task-group-68b1d021fc83947d` is `running`; workers `task-worker-73f5e2b144df7749` (T-01) and `task-worker-6f3027a0f92b2d30` (T-02) are both `running`, and both `workspace_path` values end in `\relay-dev`. |
| 2026-05-13 17:29 JST | Stale task-group recovery was fixed and verified. | `app/cli.ps1` now runs `Repair-StaleTaskGroupWorkerState` before task-group packaging; `app/core/run-state-store.ps1` returns tasks from stale/failed task groups to `ready`/`not_started` based on dependencies. Passed: `tests/task-group-parallel-recovery.ps1`, `tests/task-group-parallel-package.ps1`, `tests/task-parallelization-ready-ui.ps1`. |

## Unresolved / Watch

| Item | Status | Next action |
| --- | --- | --- |
| Phase5 T-01/T-02 task group | Ready, awaiting cautious opt-in | `task-group-68b1d021fc83947d` is now `stale` for audit. T-01/T-02 are back in the ready queue, and `show` reports `dispatch_state: awaiting_cautious_parallel_opt_in`. |
| Provider capacity | Unresolved external blocker | T-02 has now hit `Selected model is at capacity` on repeated cautious task-group attempts. Do not auto-retry in a tight loop; retry with `-AllowCautiousParallelJob` only when capacity is expected to have recovered or a model/provider fallback is chosen. |
| First Phase5 task group | Resolved as failed/reset | `task-group-71bacc353aeeaa17` is intentionally left `partial_failed` for audit history after the workspace-path bug; do not resume these workers. |
| Phase3 carry-forward requirements | Resolved in design; watch implementation | Latest Phase3-1 verdict is `go` with empty `must_fix`; ensure Phase4 implements the eight accepted contracts despite `run-state.json` still carrying the historical `open_requirements` array. |
| Phase4 carry-forward requirement: `-OutputRoot` / `result.json` acceptance | Open in current Phase4 | Ensure T-02 acceptance and tests state that `scripts/provider-smoke.ps1` does not expose `-OutputRoot` and that `logs/provider-smoke/<smoke_id>/result.json` required fields are fixed. |
| Phase4 carry-forward requirement: raw-log redaction owner | Open in current Phase4 | Ensure T-01 or T-02 owns verification that `DisableRunTracking` isolated mode writes redacted `logs/jobs/<job_id>/stdout.log` and `stderr.log` without persisting secrets. |
| Phase4 carry-forward requirement: manual live smoke owner | Open in current Phase4; revised task contract under review | Ensure T-03 or T-05 owns one provider-available smoke run with command, evidence path, and failure handling. Current revised task contract assigns this to T-03. |
| Phase4 carry-forward requirement: parallel-capable lane owner/defer | Open in current Phase4; revised task contract under review | Ensure the task contract either verifies a parallel-capable run reaches `task_lane.mode = parallel` / `max_parallel_jobs > 1`, or explicitly defers with rationale. Current revised task contract assigns documentation/verification to T-05. |
| Phase4 warning: auth heuristic | Watch | Treat `auth_required` as a helper classification only; final evidence should come from redacted stderr and `logs/provider-smoke/<smoke_id>/result.json`. |
| Phase4 warning: stdout noise | Watch | Ensure provider banner/progress noise is filtered before evaluating the first non-empty response line for `stdin_verified`. |
| Phase4 warning: resource locks | Watch | If Phase4 fans out later, confirm `changed_files` and `resource_locks` do not collide. |
| Unexpected `fake-provider` events in current run log | Watch | If later recovery or validation logic is confused by these events, trace the writer and add isolation so regression/fake-provider activity cannot append to a real run. |
| Human approval | Not currently pending | If `pending_approval` appears, inspect proposed action in `watch-run.ps1` / `cli.ps1 show` before approving or skipping. |

## Current Snapshot

As of 2026-05-13 17:30 JST:

- `run_id`: `run-20260512-164852`
- `status`: `running`
- `current_phase`: `Phase5`
- `current_role`: `implementer`
- `current_task_id`: `null`
- `active_job_id`: `null`
- `pending_approval`: `null`
- `open_requirements`: 12 total carry-forward requirements visible in state; 8 historical Phase3 requirements plus 4 Phase4 task-contract requirements
- `task_order`: `T-01`, `T-02`, `T-03`, `T-04`, `T-05`; `T-01` and `T-02` are `ready`; `T-03` waits on both
- lane: `parallel`, `max_parallel_jobs: 2`, `dispatch_state: awaiting_cautious_parallel_opt_in`, `ready_count: 2`
- active task-group workers: none
- failed/stale workers: `task-worker-6f3027a0f92b2d30` for T-02 failed on provider capacity; `task-worker-73f5e2b144df7749` for T-01 is stale; both are retained under `task-group-68b1d021fc83947d` for audit
