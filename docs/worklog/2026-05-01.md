# Worklog 2026-05-01

## 10:15 JST - Copilot起動失敗の修正を整理してpush準備

- Summary: `Phase6/T-07` で発生した `pwsh` の「ファイル名または拡張子が長すぎます」失敗に対し、Copilot provider を native `copilot.exe` 優先に変更し、process start failure を `provider_error` に正規化、`pid` を持たない stale dispatched job を自動回収できるようにした。あわせて docs 差分を整理し、`main` へ載せる準備を進めた。
- Changed: `app/execution/providers/copilot.ps1`, `app/execution/execution-runner.ps1`, `app/core/workflow-engine.ps1`, `tests/regression.ps1`, `README.md`, `docs/architecture/redesign-design-spec.md`, `docs/guide/README.md`, `docs/ideas/portfolio-improvement-backlog.md`, `docs/worklog/current.md`, `docs/worklog/2026-04-28.md`, `docs/worklog/2026-04-30.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1` は `All regression tests passed.`。`pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` は `No manifest-backed public examples found; skipping public example checks.`。
- Remaining: `config/settings.yaml`, `outputs/*`, `tasks/task.md` はローカル運用差分として push 対象外。run 自体の再開確認は未実施。

## 10:00 JST - Open Requirementsのprompt注入を軽量化

- Summary: `open_requirements` を毎回全文 prompt に埋め込む実装をやめ、task-scoped phase では `global + current task`、run-scoped phase では `global + current phase` に絞って扱うよう変更した。全文は job-scoped の `open_requirements_context.json` に退避し、prompt には要約と参照パスだけを載せる形へ切り替えた。
- Changed: `app/cli.ps1`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File .\app\cli.ps1 show` は成功。dot-source 後の `Get-RelevantOpenRequirementsForPrompt -PhaseName 'Phase6' -TaskId 'T-07'` は `Total=20, Relevant=5, Omitted=15`。preview job で `New-OpenRequirementsPromptSection` を実行し、job-scoped `open_requirements_context.json` の生成を確認後に preview job directory を削除した。
- Remaining: 実ランの再実行は未実施。Copilot provider 自体はまだ `argv` prompt mode なので、必要なら別途 `stdin` または file-based prompt への切替を検討する。

## 10:05 JST - Implementer向けにfull unresolved requirement参照を追加

- Summary: filtered な `Relevant Open Requirements` だけでは implementer/repairer が carry-forward の残件全体を能動的に解消しづらいため、job-scoped の `open_requirements_full.json` を追加で出力し、prompt summary から full unresolved requirement への参照も常時渡すようにした。implementer/repairer には full artifact を見て解消可能な項目を拾うよう誘導文も追加した。
- Changed: `app/cli.ps1`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: preview job で `Phase5/T-08/implementer` と `Phase6/T-07/reviewer` の `New-OpenRequirementsPromptSection` を実行し、`open_requirements_context.json` と `open_requirements_full.json` の両方が job-scoped に生成されることを確認。implementer summary に `if you can resolve carry-forward items...` 行が出ることも確認した。
- Remaining: 実ランの再実行は未実施。`open_requirements` の実際の解消は引き続き artifact 側の `resolved_requirement_ids` 記録に依存する。

## 10:31 JST - Phase5 promptにopen requirements回収方針を追加

- Summary: `Phase5` の runtime prompt に、現在 task の `changed_files`・`acceptance_criteria`・`boundary_contract` の範囲で解消できる `open_requirements` は可能な限りこのフェーズで回収する、という implementer 向け方針を追加した。境界外の項目だけを `known_issues` に理由付きで残すよう明確化した。
- Changed: `app/prompts/phases/phase5.md`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `Select-String -Path .\\relay-dev\\app\\prompts\\phases\\phase5.md -Pattern 'Open Requirements|open requirement|open_requirements|今回の実装で回収|今回見送った理由' -Context 1,1` で追加文言を確認。`git -C .\\relay-dev diff -- app/prompts/phases/phase5.md docs/worklog/current.md` で意図した差分のみであることを確認。
- Remaining: この変更が効くのは次に dispatch される `Phase5` job から。すでに実行中の `Phase6` job には反映されない。

## 10:31 JST - Phase5 / Phase5-1にopen requirements付きfew-shotを追加

- Summary: `Phase5` 実装 prompt と `Phase5-1` レビュー prompt の inline few-shot に、`Relevant Open Requirements` を受け取った task をどう扱うかの具体例を追加した。実装側では「in-scope は回収、out-of-scope は `known_issues` へ理由付きで残す」例、レビュー側では「in-scope の取りこぼしを reject 根拠にし、out-of-scope は warning 扱いに留める」例を入れた。
- Changed: `app/prompts/phases/phase5.md`, `app/prompts/phases/phase5-1.md`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `Get-Content .\\relay-dev\\app\\prompts\\phases\\phase5.md | Select-Object -Skip 248 -First 110` と `Get-Content .\\relay-dev\\app\\prompts\\phases\\phase5-1.md | Select-Object -Skip 190 -First 140` で追加 few-shot と code fence を確認。`git -C .\\relay-dev diff -- app/prompts/phases/phase5.md app/prompts/phases/phase5-1.md docs/worklog/current.md` で差分確認。
- Remaining: 変更は prompt asset のみで、すでに dispatch 済みの job には反映されない。次回以降の `Phase5` / `Phase5-1` 実行から有効。

## 10:56 JST - task-scoped open requirement overlayをengine生成

- Summary: `relevant open requirements` から task-scoped の overlay を生成し、`Selected Task.open_requirement_overlay` と job artifact `open_requirement_overlay.json` に注入する実装を追加した。overlay は `item_id`、`additional_acceptance_criteria`、`verification`、`suggested_changed_files` を持ち、raw の relevant requirement を boundary 内で扱いやすい補助 contract に蒸留する。
- Changed: `app/cli.ps1`, `app/prompts/system/implementer.md`, `app/prompts/system/reviewer.md`, `app/prompts/phases/phase5.md`, `app/prompts/phases/phase5-1.md`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File .\\app\\cli.ps1 show` は成功。PowerShell preview で `New-SelectedTaskOpenRequirementOverlay` を `T-07/Phase5` に対して実行し、`sec-input-validation-T-07-*` と `ui-error-path-tests-T-07` を含む overlay item を生成できることを確認後、preview job directory を削除。さらに `New-EnginePromptText` preview で `Selected Task` に `open_requirement_overlay` と overlay item_id が埋め込まれることを確認した。
- Remaining: run-state 上の実際の解消は引き続き downstream verdict artifact の `resolved_requirement_ids` 依存。synthetic prompt preview では `archived_context` の与え方が簡略化されていたため `job-context-builder.ps1` 由来の non-fatal warning が出たが、overlay 注入結果自体は確認済み。

## 11:04 JST - open requirements改善案をdocsへ追加

- Summary: `Phase7` を最終 closure authority として維持しつつ、carry-forward を前段へより canonical に配る改善方針を `docs/ideas` に提案として追加した。現行の CLI heuristic overlay を短期策と位置づけ、将来的には structured `open_requirements[]` を engine が task-scoped に投影する形へ寄せる案を整理した。
- Changed: `docs/ideas/open-requirements-carry-forward-improvement.md`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `Get-Content .\\docs\\ideas\\open-requirements-carry-forward-improvement.md` で文面確認。`git diff --check -- docs/ideas/open-requirements-carry-forward-improvement.md docs/worklog/current.md` は改行警告のみで diff error なし。
- Remaining: 提案 doc のみで、engine / validator / phase prompt への追実装は未着手。

## 11:12 JST - Copilot native path regressionをcross-platform化

- Summary: GitHub Actions の Linux で `Get-CopilotNativeCommandPath` が synthetic Copilot wrapper install を解決できず `tests/regression.ps1` が落ちていたため、Copilot provider の native package path 解決を OS 非依存の path 組み立てへ修正し、regression test も current platform に応じた package 名と binary 名を使うよう更新した。
- Changed: `app/execution/providers/copilot.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File .\\tests\\regression.ps1` は `All regression tests passed.`。
- Remaining: CI での再確認は未実施。ローカル差分の `config/settings.yaml`, `outputs/*`, `tasks/task.md` は今回の修正対象外。

## 14:40 JST - Claude/Copilotのprompt transportをstdinへ切替

- Summary: `docs/evaluations/relay-dev-evaluation-2026-04-30.md` の指摘どおり、Claude/Copilot provider の prompt transport を `argv` から `stdin` へ切り替え、Windows のコマンドライン長制限による `provider_error` を回避した。parent が TODO を管理しつつ worker を並列投入し、最後の回帰確認と run 再開は親で実施した。
- Changed: `app/execution/providers/claude.ps1`, `app/execution/providers/copilot.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File .\\tests\\regression.ps1` は `All regression tests passed.`。`pwsh -NoLogo -NoProfile -File .\\start-agents.ps1 -ResumeCurrent` 後に `pwsh -NoLogo -NoProfile -File .\\app\\cli.ps1 show` を実行し、run `run-20260427-102252` が `FT-security-input-validation / Phase5 / implementer` で `running` に戻ったことを確認。
- Remaining: `config/settings.yaml`, `outputs/*`, `tasks/task.md`, `docs/prompts/relay-dev-first-run-review-prompt.md` は今回の commit 対象外。resume 後の task 完了までは未確認。

## 15:00 JST - Phase5-2のwarning/go不整合を防ぐprompt修正

- Summary: run `run-20260427-102252` が `FT-security-input-validation / Phase5-2 / reviewer` で再停止したため調査したところ、reviewer job 自体は成功していたが `phase5-2_verdict.json` が `verdict: go` と `security_checks[].status = warning` を同時に出して `invalid_artifact` で落ちていた。`Phase5-2` prompt に JSON 保存前の自己チェックを追加し、warning が 1 件でもある場合は必ず `conditional_go` を選ぶよう明示してから run を resume した。
- Changed: `app/prompts/phases/phase5-2.md`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `app/core/artifact-validator.ps1` の `go verdict requires security_checks to be pass or not_applicable only.` 契約と、失敗 artifact `runs/run-20260427-102252/jobs/job-20260501144832-phase5-2-reviewer/artifacts/attempts/attempt-0001/tasks/FT-security-input-validation/Phase5-2/phase5-2_verdict.json` の `go + warning` を突合した。`pwsh -NoLogo -NoProfile -File .\\start-agents.ps1 -ResumeCurrent` 後に `pwsh -NoLogo -NoProfile -File .\\app\\cli.ps1 show` を確認し、run が `FT-security-input-validation / Phase5-2 / reviewer` で `running`、`active_job_id = job-20260501145943-phase5-2-reviewer` になっていることを確認。
- Remaining: prompt 修正は次の reviewer job 以降に効く。resume 後の reviewer job 完了までは未確認。

## 15:40 JST - Phase6 reject遷移とinvalid_transition recoveryを修正

- Summary: run `run-20260427-102252` の再停止原因を調査し、`Phase6` reviewer artifact は valid なのに `reject` 遷移だけ engine 契約から抜け落ちていたことと、`invalid_transition` failed state が `resume/step` で recover 対象外だったことを修正した。あわせて `Phase6` prompt / validator / regression を揃え、`attendance/route.ts` の `dateFrom` / `dateTo` Invalid Date ガード修正まで run を前進させた。
- Changed: `app/cli.ps1`, `app/core/artifact-validator.ps1`, `app/phases/phase-registry.ps1`, `app/phases/phase6.ps1`, `app/prompts/phases/phase6.md`, `tests/regression.ps1`, `docs/worklog/current.md`, `docs/worklog/2026-05-01.md`
- Verified: `pwsh -NoLogo -NoProfile -File .\\tests\\regression.ps1` は `All regression tests passed.`。続けて `pwsh -NoLogo -NoProfile -File .\\app\\cli.ps1 step` を 2 回実行し、1 回目で `Phase6 reject -> Phase5` 遷移、2 回目で `FT-security-input-validation` の `attendance/route.ts` / `attendance/route.test.ts` 修正と `430 tests passed` を確認した。最後に `pwsh -NoLogo -NoProfile -File .\\app\\cli.ps1 show` で run が `status=running`, `current_phase=Phase5-1`, `current_task_id=FT-security-input-validation`, `active_job_id=job-20260501153944-phase5-1-reviewer` になっていることを確認した。
- Remaining: run 自体は継続中で未完了。ローカル差分の `config/settings.yaml`, `outputs/*`, `tasks/task.md`, `docs/prompts/relay-dev-first-run-review-prompt.md` は今回の修正対象外。
