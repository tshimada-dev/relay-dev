# Worklog 2026-04-30

## 20:05 JST - validation を read-only 化し、legacy artifact sync を固定

- Summary: `Resolve-StepValidation` の canonical write-back を外し、validator を read-only に寄せた。あわせて `Phase7` の legacy shape artifact から repair task を復元する経路を in-memory 正規化ベースへ切り替えた。
- Changed: `app/core/artifact-validator.ps1`, `app/cli.ps1`, `app/core/workflow-engine.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`
- Remaining: `attempt-scoped staging -> validate -> commit` の本体はまだ未着手。次は `app/core/artifact-repository.ps1` と `app/cli.ps1` で canonical active artifact から staging を分離する。

## 21:34 JST - job-scoped staging と post-run commit を最小導入

- Summary: engine-dispatched phase の Required Outputs を `job-scoped staging` に向け、artifact completion probe / output sync / validation / commit を staging 経由へ切り替えた。recovered rerun の provider 回帰も prompt から staging path を読んで出力する方式へ更新した。
- Changed: `app/core/artifact-repository.ps1`, `app/cli.ps1`, `app/execution/execution-runner.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`
- Remaining: staging はまだ `attempt-scoped` ではなく `job-scoped`。retry ごとの clean staging と commit service 切り出しは次の残件。

## 22:44 JST - attempt-scoped staging と rerun archived context を整合化

- Summary: worker を並列投入して `attempt-scoped staging` の配線を進め、`job-context-builder` / `phase-validation-pipeline` / `phase-completion-committer` を追加した。さらに rerun 時に archive 後の最新 JSON snapshot を prompt context に載せる順序へ直し、recovered rerun が `RequestApproval` に戻ることを回帰で固定した。
- Changed: `app/cli.ps1`, `app/core/artifact-repository.ps1`, `app/core/job-context-builder.ps1`, `app/core/phase-validation-pipeline.ps1`, `app/core/phase-completion-committer.ps1`, `app/execution/execution-runner.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`; `pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` (`No manifest-backed public examples found; skipping public example checks.`)
- Remaining: `phase completion transaction` の service 化と、`Apply-JobResult` 周辺の state/event 更新責務の整理はまだ残っている。

## 10:30 JST - active_attempt と phase execution transaction を統合

- Summary: worker を並列で使って `attempt-preparation`、`phase-execution-transaction`、`active_attempt` helper を作り、親側で `cli.ps1` に統合した。dispatch 前 archive と latest archived JSON context、attempt-scoped execution/validate/commit、`active_attempt` の `dispatching -> running -> committing -> clear` を 1 本の流れにまとめ、task-aware な `phase_history` も追加した。
- Changed: `app/cli.ps1`, `app/core/attempt-preparation.ps1`, `app/core/phase-execution-transaction.ps1`, `app/core/run-state-store.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`; `pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` (`No manifest-backed public examples found; skipping public example checks.`)
- Remaining: None for the current phase-transition refactor goal.

## 12:55 JST - self-run interruption の原因を修正して Phase5-2 から再開

- Summary: `run-20260427-102252` の自走中断を調査し、原因が `Phase5-2` reviewer artifact の validator 不整合だと特定した。`conditional_go` verdict に `security_checks[].status = fail` を含めたため `invalid_artifact` で停止していたので、reviewer prompt の判定規則を validator に合わせて修正し、provider job 自体は成功している `invalid_artifact` run も `resume` で回復できるようにした。そのうえで visible worker を `ResumeCurrent` で再起動し、`Phase5-2` reviewer として run を再開した。
- Changed: `app/prompts/phases/phase5-2.md`, `app/cli.ps1`, `tests/regression.ps1`, `docs/worklog/current.md`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`; `pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` (`No manifest-backed public examples found; skipping public example checks.`); `pwsh -NoLogo -NoProfile -File .\start-agents.ps1 -ResumeCurrent`; `pwsh -NoLogo -NoProfile -File .\app\cli.ps1 show`
- Remaining: resumed run の `Phase5-2` reviewer (`job-20260430124816-phase5-2-reviewer`) が verdict を生成し、`Phase6` 以降へ遷移するかを canonical state で追跡する。

## 13:25 JST - rerun prompt staging assertion を OS 非依存に修正

- Summary: CI の `tests/regression.ps1` が `Recovered rerun prompts should direct outputs into the job-scoped staging area` で落ちていたため、Windows 固定の `\jobs\job-` 期待値を削除し、実装どおりの `attempt-scoped` path を `[/\\]attempts[/\\]attempt-0001` 形式で検証する OS 非依存 assertion に変更した。
- Changed: `tests/regression.ps1`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`; `pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` (`No manifest-backed public examples found; skipping public example checks.`)
- Remaining: None.

## 15:07 JST - repairer 導入計画を docs に追加

- Summary: artifact validation failure で run が即停止する体験を改善するため、`repairer` を artifact 修復専用 role として導入する実装計画を追加した。特に「product code を絶対に触らない」「review を絶対にさせない」を非交渉制約として明文化し、repairable failure の分類、repair lane、immutable field guard、repair budget まで計画化した。
- Changed: `docs/repairer-implementation-plan.md`, `docs/repairer-implementation-plan.json`, `docs/worklog/current.md`, `docs/worklog/2026-04-30.md`
- Verified: `Get-Content docs/repairer-implementation-plan.json -Raw | ConvertFrom-Json`
- Remaining: 実装は未着手。次は `workflow-engine` と `phase-execution-transaction` に `repairable invalid_artifact -> repair lane` の分岐を追加する。

## 15:27 JST - repairer の artifact-only repair lane を実装

- Summary: parallel worker を使って `repairer` の最小実装を landed した。`repairer` 専用 system prompt、repairability policy、immutable diff guard、repair prompt builder、artifact repair transaction を追加し、`phase-execution-transaction` と `cli.ps1` から `invalid_artifact -> repair -> revalidate -> commit` の same-phase 修復レーンへ接続した。`repairer` は staged artifact のみを触り、product code と review 判断を禁止している。
- Changed: `app/cli.ps1`, `app/core/phase-execution-transaction.ps1`, `app/core/artifact-repair-policy.ps1`, `app/core/artifact-repair-transaction.ps1`, `app/core/repair-diff-guard.ps1`, `app/core/repair-prompt-builder.ps1`, `app/phases/phase-registry.ps1`, `app/prompts/system/repairer.md`, `tests/regression.ps1`
- Verified: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`; `pwsh -NoLogo -NoProfile -File scripts/check-public-examples.ps1` (`No manifest-backed public examples found; skipping public example checks.`)
- Remaining: repair budget はまだ single-pass 前提。次に進めるなら repair 履歴の run-state 管理と複数回 repair の circuit breaker を実装する。
