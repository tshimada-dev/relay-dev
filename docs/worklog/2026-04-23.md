# Worklog 2026-04-23

## Context

- `docs/portfolio-roadmap.md` をベースに、relay-dev の portfolio-ready 化を Phase A から開始した。
- Phase A の目標は、初回公開最低ラインとして「README 1画面目で価値が伝わる」「公開前ハードニングが通る」「強い flagship example 1本へ進める土台がある」状態に寄せること。

## Roadmap Updates

- 言語方針を明確化した。
  - agent-facing instruction は英語。
  - few-shot / output examples / Markdown artifact の文体サンプルは日本語。
  - JSON keys / schema fields / phase names / task IDs / paths / commands / code identifiers は英語または原文維持。
- `Release Slicing` を追加し、Phase A / B / C に分割した。
  - Phase A: 公開最低ライン。
  - Phase B: 見栄え強化。
  - Phase C: 2本目 example、動画、release 運用などの拡張。
- 初回公開の必須 example は flagship 1本に絞り、2本目や動画は後追いに変更した。
- README 冒頭で非対象を明示する方針を追加した。
- 公開 example に manifest、成果指標、sanitize / validator / scanner を要求する方針を追加した。

## Implementation

- PowerShell 7 (`pwsh`) を正式 runtime として明確化した。
  - `app/cli.ps1`
  - `agent-loop.ps1`
  - `start-agents.ps1`
  - `watch-run.ps1`
  - `scripts/check-public-examples.ps1`
- Phase0 seed freshness guard を追加した。
  - `phase0_context.json` に `task_fingerprint`、`task_path`、`seed_created_at` を必須化。
  - `app/cli.ps1` の `SeedPhase0` import 前に現在の `tasks/task.md` SHA-256 と照合。
  - 不一致または metadata 不足の場合、古い seed を静かに再利用せず失敗させる。
- `start-agents.ps1` / `start-agents.sh` を canonical-first に寄せた。
  - `runs/current-run.json` / `run-state.json` を優先。
  - `queue/status.yaml` は canonical が見つからない場合の legacy fallback として扱う。
- 公開 example 用 scanner を追加した。
  - `scripts/check-public-examples.ps1`
  - manifest-backed example のみを対象に、raw `runs/`、job logs、絶対パス、secret らしき文字列を検出。
  - CI に scanner 実行を追加。
- README を Phase A 向けに更新した。
  - 短い英語要約を追加。
  - `5分で見る relay-dev` を追加。
  - 非対象を追加。
  - 手動コマンドを `pwsh -NoLogo -NoProfile -File ...` に統一。
  - legacy `gemini_video_plugin` を portfolio proof として扱わない旨を明記。
- repository metadata を追加した。
  - `LICENSE`
  - `CONTRIBUTING.md`
  - `SECURITY.md`
  - `CHANGELOG.md`
- `examples/README.md` を追加し、公開用 examples の条件を明文化した。

## Tests

- PowerShell syntax check: passed.
- Public example scanner: passed.
  - 現時点では manifest-backed public example がないため skip。
- Regression test: passed.
  - Command: `pwsh -NoLogo -NoProfile -File tests/regression.ps1`
  - Result: `All regression tests passed.`
- Whitespace check: passed.
  - Command: `git diff --check`

## Notes

- `docs/portfolio-roadmap.md` はまだ untracked。
- Phase A の残タスクは、現行 run 由来の強い flagship example 1本を作成し、sanitize 済み example として追加すること。
- `examples/gemini_video_plugin` はまだ残っているが、現行 architecture の portfolio proof ではない扱いに変更済み。

## 17:49 JST - Worklog Skill And AGENTS.md

- Summary: 作業後の worklog 記録を常時運用にするため、repo-local `worklog` skill と `AGENTS.md` を追加した。
- Changed: `skills/worklog/SKILL.md`、`skills/worklog/agents/openai.yaml`、`AGENTS.md`。
- Verified: `quick_validate.py` で `skills/worklog` が valid、`git diff --check -- AGENTS.md skills/worklog docs/worklog/2026-04-23.md` が pass。
- Remaining: 今後の作業では `AGENTS.md` に従い、final response 前に `docs/worklog/YYYY-MM-DD.md` を更新する。

## 17:55 JST - Current Handoff 運用追加

- Summary: compaction 復帰を速くするため、作業途中用の `docs/worklog/current.md` と daily worklog を分ける運用を追加した。
- Changed: `AGENTS.md`、`skills/worklog/SKILL.md`、`skills/worklog/agents/openai.yaml`、`docs/worklog/current.md`。
- Verified: `quick_validate.py` で `skills/worklog` が valid、`git diff --check -- AGENTS.md skills/worklog docs/worklog` が pass。
- Remaining: 今後の複数ステップ作業では、方針確定後や大きな編集前に `docs/worklog/current.md` を更新する。
